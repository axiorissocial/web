# Production Environment Variables

# Backend API URL (required)
# This should point to your production backend API
VITE_API_URL=https://api.axioris.omgrod.me

# HMR is not used in production builds, but if you're testing the build
# with 'yarn preview', these can be left empty or removed

# Optional: Custom build configurations
# NODE_ENV=production
